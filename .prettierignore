node_modules
gen
@cds-models
db.sqlite

# CSV seed data is column-aligned on purpose - do not reformat
db/data

# Generated by release-please on every release. Prettier would reflow it and release-please would
# write it straight back, so the format check would fail on each release PR.
CHANGELOG.md
